# PvP

```py
Dnd dnd = Dnd()

dnd.get_intro() -> str # returns the intro to start the story
dnd.user_submit_text(username, text) -> str, img
dnd.get_options(username)
dnd.get_latest_health_update(username) -> int
dnd.add_user(username)
```